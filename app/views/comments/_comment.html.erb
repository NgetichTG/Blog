<div id="<%= dom_id comment %>">
  <p>
    <strong>
      Posted <%=time_ago_in_words(comment.created_at)%>
    </strong>
    <%= comment.post_id %>
  </p>

  <p>
    <strong>Body:</strong>
    <%= comment.body %>
  </p>

</div>
